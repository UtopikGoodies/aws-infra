# Account requests

Create one folder per account request.

Example:

```
account-requests/
  nonprod-shared-services/
    request.auto.tfvars.json
```

Use the template in `template/request.auto.tfvars.json` as a starting point.

Validate account naming, email ownership, and target OU before opening pull requests.
